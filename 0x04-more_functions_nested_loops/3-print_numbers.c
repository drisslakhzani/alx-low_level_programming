#include "main.h"

/*
 * print_numbers - func checks for checks for a digit (o through 9).
 *
 * Return: Always 0 
 */
void print_numbers(void)
{
	int c;

	for (c = 40; c < 58; c++)
	{
		_putchar(c);
	}
		_putchar('\n');
}
